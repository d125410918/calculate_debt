async function fetchGoldPrice(){const status=document.getElementById('goldStatus');try{if(status)status.textContent='讀取中';const res=await fetch('https://api.gold-api.com/price/XAU');if(!res.ok)throw new Error('fetch');const data=await res.json();const value=Math.round((Number(data.price||0)/31.1035)*3.75);if(status)status.textContent='已取得';return value>0?value:0;}catch(e){if(status)status.textContent='請手動輸入';return 0;}}
